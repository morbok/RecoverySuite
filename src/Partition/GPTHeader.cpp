#include "RecoverySuite/Partition/GPTHeader.hpp"
#include <cstring>

namespace recoverysuite {
namespace partition {

// GPTHeader is a trivial struct with no dynamic data, so no implementation needed beyond the header.
// The constructor is implicitly defined.
// The methods are defined in the header.

} // namespace partition
} // namespace recoverysuite